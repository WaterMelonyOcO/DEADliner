[on unit test]

1) после тестирования не правильно работало удаление елементов{
    #edit не правильно записываются елементы в бд
    $ыстатус: исправлено$
}
2) после добавления и удаления в бд задания, бд ломается{
    похоже это происходит из-за ошибки удаления папки, а срабоатывающее исключение 
    не даёт возможность вернуть true для интерфейса, из-за чего удалённое задание остаётся
    
    *примернон исправление*
    поменял местами и при ошибке кдаления папки всё равно возвращаю true

    $статус: истправить$
}