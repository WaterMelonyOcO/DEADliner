{
  "name": "elec",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-packager . --out=build --overwrite --platform=win32"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "electron": "^23.2.1",
    "luxon": "^3.3.0"
  },
  "devDependencies": {
    "@electron/build-tools": "^1.1.0",
    "electron-packager": "^17.1.1",
    "nodemon": "^2.0.22"
  }
}
